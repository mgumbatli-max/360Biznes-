/**
 * Default search-bar secret code. Owner can change via /sahibkar/ayarlar.
 * Typing this string in the global search palette redirects to the sahibkar
 * entrance even when the sidebar item is hidden.
 */
export const DEFAULT_SECRET_CODE = "7733";
